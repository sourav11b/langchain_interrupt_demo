# ── VaultIQ runtime dependencies ──────────────────────────────────────────────
# Core LangChain stack — pinned to 1.x for langchain-mongodb AutoEmbeddings.
langchain>=1.0,<2.0
langchain-core>=1.2.5,<2.0
langchain-classic>=1.0,<2.0
langchain-text-splitters>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-community>=0.4,<0.5
# Voyage embedding now runs server-side via Atlas AutoEmbeddings, so the
# langchain-voyageai client package (no langchain-core 1.x release) is no
# longer required.

# MongoDB integrations — 0.11.0 introduces AutoEmbeddings.
langchain-mongodb>=0.11.0,<0.12
pymongo>=4.10.0

# LangGraph orchestration + Mongo checkpoint/store
langgraph>=1.0,<2.0
langgraph-checkpoint-mongodb>=0.3.0
langgraph-store-mongodb>=0.1.0

# Deep Agents (a2a / sub-agents pattern)
deepagents>=0.0.10

# MCP integration (MongoDB MCP server bridge)
langchain-mcp-adapters>=0.2.0
mcp>=1.2.0

# LangSmith observability
langsmith>=0.2.0

# UI — NiceGUI (FastAPI + Vue) replaced Streamlit for stability under
# long-running agent calls and reactive case/timeline updates.
nicegui>=2.0.0
plotly>=5.24.0

# Data + utilities
requests>=2.32.0          # used by Atlas Admin API client (cluster autoresume)
pandas>=2.2.0
numpy>=1.26.0
faker>=30.0.0
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.6.0
tenacity>=9.0.0
